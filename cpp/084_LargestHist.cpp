class Solution {
public:
    int largestRectangleArea(vector<int>& heights) {
    	int l = heights.size();
    	vector<int> right(l);
    	vector<int> left(l);
        for(int i=0;i<l;i++){
        	left[i] = 0;
        }
    }
};